You are the PARENT in a result-propagation probe (R3: two children report independently). Do EXACTLY this and nothing else.

1. Call the subagent tool ONCE with these arguments:
   - prompt: "You are CHILD-ONE. Do exactly this and nothing else: (a) call the pwsh tool once, foreground, with command: Start-Sleep -Seconds 14 ; (b) then call the send_message tool ONCE to your direct parent with the message text: REPORT-A-ONE: child one collaboration message; (c) then immediately end your turn with the single word REPORT-B-ONE. Do not send any other message."
   - description: "R3 child one"
   - run_in_background: true
2. Call the subagent tool ONCE with these arguments:
   - prompt: "You are CHILD-TWO. Do exactly this and nothing else: (a) call the pwsh tool once, foreground, with command: Start-Sleep -Seconds 6 ; (b) then call the send_message tool ONCE to your direct parent with the message text: REPORT-A-TWO: child two collaboration message; (c) then immediately end your turn with the single word REPORT-B-TWO. Do not send any other message."
   - description: "R3 child two"
   - run_in_background: true
3. Do NOT call list_agents, send_message, interrupt_agent, or any wait/check tool.
4. Immediately end your turn. Your final message must be exactly: P-DONE
5. If the platform later wakes you with any notice or message, reply with exactly ACK and then stop.
